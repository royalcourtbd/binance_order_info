name: Android Release Build

on:
  push:
    branches:
      - build # build branch এ push হলে
    tags:
      - "v*.*.*" # অথবা tag করলে

permissions:
  contents: write
  actions: read
  checks: write

jobs:
  build-android-release:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: "zulu"
          java-version: "17"
          cache: "gradle"

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.38.5" # Latest version
          cache: true

      - name: Cache Flutter dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.pub-cache
            ~/.gradle/caches
            ~/.gradle/wrapper
            android/.gradle
            android/.android
          key: ${{ runner.os }}-flutter-gradle-${{ hashFiles('**/pubspec.lock', '**/gradle-wrapper.properties', '**/*.gradle*') }}
          restore-keys: |
            ${{ runner.os }}-flutter-gradle-
            ${{ runner.os }}-flutter-

      - name: Install dependencies
        run: flutter pub get

      - name: Run tests
        run: flutter test
        continue-on-error: true

      - name: Build APK (Release)
        run: flutter build apk --release --split-per-abi --obfuscate --split-debug-info=build/app/outputs/symbols

      - name: Build AAB (Release)
        run: flutter build appbundle --release --obfuscate --split-debug-info=build/app/outputs/symbols

      - name: Verify build outputs
        run: |
          echo "📦 Build outputs:"
          echo "==== APK Files ===="
          ls -lh build/app/outputs/flutter-apk/
          echo ""
          echo "==== AAB File ===="
          ls -lh build/app/outputs/bundle/release/
          echo ""
          echo "==== Debug Symbols ===="
          ls -lh build/app/outputs/symbols/
          echo ""

          # Split-per-abi creates multiple APKs
          if [ ! -f "build/app/outputs/bundle/release/app-release.aab" ]; then
            echo "❌ AAB file not found!"
            exit 1
          fi

          echo "✅ Build outputs verified successfully"

      # Improved: Better app info extraction from Code 2
      - name: Get app info from pubspec
        id: app_info
        run: |
          # Get version (improved parsing)
          VERSION=$(grep '^version:' pubspec.yaml | sed 's/version: *//' | cut -d '+' -f 1)
          echo "VERSION=$VERSION" >> $GITHUB_OUTPUT

          # Get app name (dynamic) - fallback to repository name if not found
          if grep -q '^name:' pubspec.yaml; then
            APP_NAME=$(grep '^name:' pubspec.yaml | sed 's/name: *//' | tr '_' ' ' | sed 's/\b\w/\U&/g')
          else
            APP_NAME=$(echo "${{ github.repository }}" | cut -d'/' -f2 | tr '_-' ' ' | sed 's/\b\w/\U&/g')
          fi
          echo "APP_NAME=$APP_NAME" >> $GITHUB_OUTPUT

          # Generate changelog from commits since last tag
          PREVIOUS_TAG=$(git describe --tags --abbrev=0 HEAD~1 2>/dev/null || echo "")
          if [ -n "$PREVIOUS_TAG" ]; then
            echo "CHANGELOG<<EOF" >> $GITHUB_OUTPUT
            git log $PREVIOUS_TAG..HEAD --oneline --pretty=format:"- %s" >> $GITHUB_OUTPUT
            echo "" >> $GITHUB_OUTPUT
            echo "EOF" >> $GITHUB_OUTPUT
          else
            echo "CHANGELOG=Initial release" >> $GITHUB_OUTPUT
          fi

      - name: Calculate file sizes
        id: file_sizes
        run: |
          # Calculate individual APK sizes
          APK_SIZE=$(du -h build/app/outputs/flutter-apk/app-release.apk 2>/dev/null | cut -f1 || echo "N/A")
          AAB_SIZE=$(du -h build/app/outputs/bundle/release/app-release.aab | cut -f1)

          # For split-per-abi, get the total size
          if [ -f "build/app/outputs/flutter-apk/app-arm64-v8a-release.apk" ]; then
            TOTAL_APK_SIZE=$(du -ch build/app/outputs/flutter-apk/*.apk | grep total | cut -f1)
            APK_COUNT=$(ls -1 build/app/outputs/flutter-apk/*.apk 2>/dev/null | wc -l | tr -d ' ')
          else
            TOTAL_APK_SIZE=$APK_SIZE
            APK_COUNT=1
          fi

          # Debug symbols size
          SYMBOLS_SIZE=$(du -sh build/app/outputs/symbols/ 2>/dev/null | cut -f1 || echo "N/A")

          echo "APK_SIZE=$APK_SIZE" >> $GITHUB_OUTPUT
          echo "AAB_SIZE=$AAB_SIZE" >> $GITHUB_OUTPUT
          echo "TOTAL_APK_SIZE=$TOTAL_APK_SIZE" >> $GITHUB_OUTPUT
          echo "APK_COUNT=$APK_COUNT" >> $GITHUB_OUTPUT
          echo "SYMBOLS_SIZE=$SYMBOLS_SIZE" >> $GITHUB_OUTPUT

          echo "APK Size: $APK_SIZE"
          echo "AAB Size: $AAB_SIZE"
          echo "Total APK Size: $TOTAL_APK_SIZE"
          echo "APK Count: $APK_COUNT"
          echo "Symbols Size: $SYMBOLS_SIZE"

      # Improved: Better date formatting with timezone support
      - name: Format build date
        id: build_date
        run: |
          # Convert to Bangladesh time and format (fallback to UTC if timezone not available)
          if command -v timedatectl &> /dev/null; then
            BUILD_DATE=$(TZ='Asia/Dhaka' date +'%d %B %Y - %l:%M%p' | sed 's/AM/am/g; s/PM/pm/g; s/  / /g')
          else
            BUILD_DATE=$(date -u +'%d %B %Y - %l:%M%p UTC' | sed 's/AM/am/g; s/PM/pm/g; s/  / /g')
          fi
          echo "BUILD_DATE=$BUILD_DATE" >> $GITHUB_OUTPUT

      # Using ncipollo/release-action for better flexibility
      - name: Create Release (only for tags)
        if: startsWith(github.ref, 'refs/tags/')
        uses: ncipollo/release-action@v1
        with:
          tag: ${{ github.ref_name }}
          name: 💰 Binance P2P Order Info ${{ github.ref_name }}
          body: |
            ## 💰 Binance P2P Order Info Release ${{ github.ref_name }}

            ### 📱 Download

            | File Type | Size | Description |
            |-----------|------|-------------|
            | **APK** | ${{ steps.file_sizes.outputs.TOTAL_APK_SIZE }} | Direct install for Android devices |
            | **AAB** | ${{ steps.file_sizes.outputs.AAB_SIZE }} | Google Play Store bundle |

            ### 🔄 What's Changed

            ${{ steps.app_info.outputs.CHANGELOG }}

            ### 📋 Installation Instructions

            **For APK:**

            1. Download `app-release.apk` below
            2. Enable "Install from Unknown Sources" in Android Settings
            3. Open and install the APK

            **System Requirements:**

            • Android 7.0+ (API 24)
            • 50MB+ free storage

            ### 🛡️ Security

            • Built with Flutter 3.38.5
            • All dependencies up to date
            • Split-per-ABI optimization enabled
            • **Code obfuscation enabled** for enhanced security
            • Debug symbols preserved for crash analysis

            ---

            **App Version:** ${{ steps.app_info.outputs.VERSION }}  
            **Build Date:** ${{ steps.build_date.outputs.BUILD_DATE }}  
            **Commit:** [${{ github.sha }}](https://github.com/${{ github.repository }}/commit/${{ github.sha }})
          draft: false
          prerelease: false
          artifacts: |
            build/app/outputs/flutter-apk/*.apk
            build/app/outputs/bundle/release/app-release.aab
          token: ${{ secrets.GITHUB_TOKEN }}
          allowUpdates: true

      - name: Upload Build Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: android-release-${{ github.ref_name || github.sha }}
          path: |
            build/app/outputs/flutter-apk/*.apk
            build/app/outputs/bundle/release/app-release.aab
          retention-days: 30

      - name: Upload Debug Symbols
        uses: actions/upload-artifact@v4
        with:
          name: debug-symbols-${{ github.ref_name || github.sha }}
          path: build/app/outputs/symbols/
          retention-days: 90

      - name: Build Summary
        run: |
          echo "### ✅ Build Successful!" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Version:** ${{ steps.app_info.outputs.VERSION }}" >> $GITHUB_STEP_SUMMARY
          echo "**APK Count:** ${{ steps.file_sizes.outputs.APK_COUNT }}" >> $GITHUB_STEP_SUMMARY
          echo "**Total APK Size:** ${{ steps.file_sizes.outputs.TOTAL_APK_SIZE }}" >> $GITHUB_STEP_SUMMARY
          echo "**AAB Size:** ${{ steps.file_sizes.outputs.AAB_SIZE }}" >> $GITHUB_STEP_SUMMARY
          echo "**Symbols Size:** ${{ steps.file_sizes.outputs.SYMBOLS_SIZE }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "🛡️ **Security:** Code obfuscation enabled" >> $GITHUB_STEP_SUMMARY
          echo "📱 Build artifacts uploaded successfully!" >> $GITHUB_STEP_SUMMARY
          echo "🔍 Debug symbols preserved for crash analysis" >> $GITHUB_STEP_SUMMARY
